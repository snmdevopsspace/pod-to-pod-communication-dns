apiVersion: v1
kind: Service
metadata:
  name: snmse-auth-service
spec:
  selector: # select the pod
    app: snmse-auth
  type: ClusterIP # wont expose outside the cluster
  ports:
    - protocol: TCP
      port: 5002
      targetPort: 5002